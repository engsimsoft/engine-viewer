#!/bin/bash

# 🔄 Engine Results Viewer - Restart Script
# Перезапускает Backend и Frontend

# Определяем корень проекта
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
cd "$PROJECT_ROOT"

# Цвета для вывода
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🔄 Engine Results Viewer - Restarting..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

# Остановка
echo -e "${BLUE}Шаг 1/2: Остановка...${NC}"
echo ""
"$SCRIPT_DIR/stop.sh"

# Небольшая пауза для корректного завершения процессов
sleep 2

# Запуск
echo ""
echo -e "${BLUE}Шаг 2/2: Запуск...${NC}"
echo ""
"$SCRIPT_DIR/start.sh"

echo ""
echo -e "${GREEN}✅ Перезапуск завершён!${NC}"
echo ""
