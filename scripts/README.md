# 🚀 Скрипты управления проектом

**Дата создания:** 31 октября 2025
**Версия:** 1.0

---

## 📋 Обзор

Для удобного управления проектом Engine Results Viewer созданы 4 shell-скрипта:

| Скрипт | Назначение | Использование |
|--------|------------|---------------|
| `start.sh` | Запуск проекта | `./scripts/start.sh` |
| `stop.sh` | Остановка проекта | `./scripts/stop.sh` |
| `restart.sh` | Перезапуск проекта | `./scripts/restart.sh` |
| `status.sh` | Проверка статуса | `./scripts/status.sh` |

---

## 🚀 start.sh - Запуск проекта

### Что делает

1. ✅ Проверяет что порты 3000 и 5173 свободны
2. ✅ Устанавливает зависимости (если нужно)
3. ✅ Запускает Backend на http://localhost:3000
4. ✅ Запускает Frontend на http://localhost:5173
5. ✅ Сохраняет логи в `logs/backend.log` и `logs/frontend.log`
6. ✅ Сохраняет PIDs процессов в `.backend.pid` и `.frontend.pid`

### Использование

```bash
./scripts/start.sh
```

### Вывод

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 Engine Results Viewer - Starting...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📡 Проверка портов...
📦 Проверка зависимостей...

🔧 Запуск Backend...
   PID: 12345
   Лог: logs/backend.log
   Ожидание запуска...
   ✓ Backend запущен: http://localhost:3000

⚛️  Запуск Frontend...
   PID: 12346
   Лог: logs/frontend.log
   Ожидание запуска...
   ✓ Frontend запущен: http://localhost:5173

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Engine Results Viewer запущен успешно!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📡 Backend:  http://localhost:3000
⚛️  Frontend: http://localhost:5173

📋 Логи:
   Backend:  tail -f logs/backend.log
   Frontend: tail -f logs/frontend.log

🛑 Остановка: ./stop.sh
🔄 Перезапуск: ./restart.sh
📊 Статус:    ./status.sh
```

### Особенности

- **Автоматическая установка зависимостей**: Если `node_modules` не найдены, запустится `npm install`
- **Проверка портов**: Если порты заняты, скрипт предложит остановить старые процессы
- **Фоновый режим**: Backend и Frontend запускаются в фоне (можно продолжать работу в терминале)
- **Логирование**: Все логи сохраняются в `logs/` директории

---

## 🛑 stop.sh - Остановка проекта

### Что делает

1. ✅ Останавливает Backend по сохранённому PID
2. ✅ Останавливает Frontend по сохранённому PID
3. ✅ Проверяет и освобождает порты 3000 и 5173
4. ✅ Убивает все связанные процессы (node, vite)
5. ✅ Удаляет PID файлы

### Использование

```bash
./scripts/stop.sh
```

### Вывод

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🛑 Engine Results Viewer - Stopping...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 Остановка Backend (PID: 12345)...
   ✓ Backend остановлен

⚛️  Остановка Frontend (PID: 12346)...
   ✓ Frontend остановлен

🔍 Проверка портов 3000 и 5173...
   ✓ Порты свободны

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Engine Results Viewer остановлен
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 Запуск: ./start.sh
```

### Особенности

- **Множественные методы остановки**:
  - По сохранённым PIDs (приоритет)
  - По портам (если PIDs не найдены)
  - По именам процессов (резервный метод)
- **Безопасная остановка**: Сначала SIGTERM, затем SIGKILL
- **Очистка**: Удаляет `.backend.pid` и `.frontend.pid`

---

## 🔄 restart.sh - Перезапуск проекта

### Что делает

1. ✅ Вызывает `./stop.sh`
2. ✅ Пауза 2 секунды (корректное завершение процессов)
3. ✅ Вызывает `./start.sh`

### Использование

```bash
./scripts/restart.sh
```

### Когда использовать

- После изменений в backend коде (для перезагрузки сервера)
- После изменений в config.yaml
- При сбоях в работе приложения
- После обновления зависимостей (`npm install`)

**⚠️ Примечание**: Frontend поддерживает Hot Module Replacement (HMR), поэтому для изменений во frontend коде перезапуск не требуется.

---

## 📊 status.sh - Проверка статуса

### Что делает

1. ✅ Проверяет статус Backend (порт 3000)
2. ✅ Проверяет статус Frontend (порт 5173)
3. ✅ Проверяет health endpoint Backend
4. ✅ Показывает PIDs процессов
5. ✅ Показывает последние строки логов
6. ✅ Показывает размеры логов

### Использование

```bash
./scripts/status.sh
```

### Вывод

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 Engine Results Viewer - Status
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 Backend (http://localhost:3000):
   Статус: ✅ Работает
   PID:    12345
   Uptime: 15.3 мин
   Health: OK
   Лог:    (последние 3 строки)
           ✅ Configuration loaded successfully
              Data folder: ./test-data
              Server: localhost:3000

⚛️  Frontend (http://localhost:5173):
   Статус: ✅ Работает
   PID:    12346
   HTTP:   OK
   Лог:    (последние 3 строки)
           VITE v7.1.11  ready in 148 ms
           ➜  Local:   http://localhost:5173/
           ➜  Network: use --host to expose

📋 Сохранённые PIDs:
   Backend:  12345 (активен)
   Frontend: 12346 (активен)

📁 Логи:
   Backend:  logs/backend.log (24K)
   Frontend: logs/frontend.log (156K)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 Запуск:     ./start.sh
🛑 Остановка:  ./stop.sh
🔄 Перезапуск: ./restart.sh
```

### Когда использовать

- Проверка что приложение запущено
- Диагностика проблем с запуском
- Просмотр uptime и PID процессов
- Быстрый доступ к последним строкам логов

---

## 📁 Логи

Все логи сохраняются в директории `logs/`:

```
logs/
├── backend.log    # Лог Backend сервера
└── frontend.log   # Лог Frontend dev server
```

### Просмотр логов

```bash
# Последние 50 строк backend лога
tail -50 logs/backend.log

# Последние 50 строк frontend лога
tail -50 logs/frontend.log

# Следить за логом в реальном времени (backend)
tail -f logs/backend.log

# Следить за логом в реальном времени (frontend)
tail -f logs/frontend.log

# Очистить логи
rm logs/*.log
```

### Ротация логов

Логи автоматически перезаписываются при каждом запуске `./start.sh`. Для долгосрочного хранения логов можно настроить ротацию:

```bash
# Ручная ротация (сохранение старых логов)
mv logs/backend.log logs/backend-$(date +%Y%m%d-%H%M%S).log
mv logs/frontend.log logs/frontend-$(date +%Y%m%d-%H%M%S).log
```

---

## 🔧 PID файлы

Скрипты сохраняют PIDs процессов в файлы:

- `.backend.pid` - PID backend процесса
- `.frontend.pid` - PID frontend процесса

**⚠️ Не редактируйте эти файлы вручную!**

Они автоматически:
- Создаются при запуске (`./start.sh`)
- Удаляются при остановке (`./stop.sh`)
- Игнорируются Git (`.gitignore`)

---

## 🐛 Troubleshooting

### Проблема: "Порт уже занят"

**Причина**: Backend или Frontend уже запущены (или другое приложение занимает порт)

**Решение**:
```bash
# Остановить старые процессы
./scripts/stop.sh

# Или вручную
lsof -i :3000 | grep LISTEN | awk '{print $2}' | xargs kill
lsof -i :5173 | grep LISTEN | awk '{print $2}' | xargs kill
```

---

### Проблема: "Backend не отвечает"

**Причина**: Backend запустился, но не работает корректно

**Решение**:
```bash
# Проверить лог backend
tail -50 logs/backend.log

# Проверить health endpoint
curl http://localhost:3000/health

# Перезапустить
./scripts/restart.sh
```

---

### Проблема: "Frontend показывает ошибку сборки"

**Причина**: TypeScript ошибки или проблемы с зависимостями

**Решение**:
```bash
# Проверить лог frontend
tail -50 logs/frontend.log

# Переустановить зависимости
cd frontend
rm -rf node_modules package-lock.json
npm install

# Запустить снова
cd ..
./scripts/restart.sh
```

---

### Проблема: "Скрипт не запускается"

**Причина**: Нет прав на выполнение

**Решение**:
```bash
# Установить права на выполнение
chmod +x start.sh stop.sh restart.sh status.sh

# Или для всех скриптов сразу
chmod +x *.sh
```

---

## 🔗 Ссылки

- [README.md](../README.md) - Главная документация
- [docs/setup.md](../docs/setup.md) - Детальная установка
- [docs/troubleshooting.md](../docs/troubleshooting.md) - Решение проблем
- [config.yaml](../config.yaml) - Конфигурация проекта

---

**Скрипты готовы к использованию! 🚀**
