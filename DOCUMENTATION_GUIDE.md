# Руководство по ведению документации проекта

> **Важно:** Этот документ описывает правила и принципы ведения документации Engine Results Viewer.
> При любых изменениях в проекте следуй этим правилам!

---

## 🚨 ОБЯЗАТЕЛЬНОЕ ПРАВИЛО ДЛЯ ВСЕХ ИЗМЕНЕНИЙ

### После ЛЮБЫХ значительных изменений документация ОБЯЗАНА быть обновлена!

**Что считается "значительными изменениями":**
- ✅ Создание новых файлов (парсеры, роуты, компоненты)
- ✅ Изменение архитектуры (Backend/Frontend)
- ✅ Добавление или удаление npm зависимостей
- ✅ Изменение API endpoints
- ✅ Изменение структуры папок
- ✅ Добавление новых функций/фич (графики, таблицы, экспорт)
- ✅ Изменение формата данных (.det файлы)
- ✅ Завершение крупных фич

**Что нужно сделать:**
1. ✅ Обновить [docs/architecture.md](docs/architecture.md) (SSOT для архитектуры, API, parsers)
2. ✅ Пройти чек-лист (см. раздел "Чек-лист при изменениях")
3. ✅ Проверить, что нигде нет дублирования информации
4. ✅ Обновить [CHANGELOG.md](CHANGELOG.md) (история изменений)
5. ✅ Создать ADR если принято архитектурное решение (см. [docs/decisions/](docs/decisions/))

**Это НЕ рекомендация, это ОБЯЗАТЕЛЬНОЕ требование!**

> 💡 Неактуальная документация хуже, чем отсутствие документации.
> Всегда обновляй документы после изменений!

---

## 🎯 Принципы документации

### 1. Single Source of Truth (SSOT)
- Каждая информация живёт в ОДНОМ месте
- Остальные файлы ссылаются на неё
- Обновил в одном месте → везде актуально

**Пример:**
- ❌ Неправильно: Писать формат .det файлов в README, CLAUDE.md, и архитектуре
- ✅ Правильно: Написать в [docs/file-formats/det-format.md](docs/file-formats/det-format.md), остальные ссылаются на него

### 2. Consolidation over Proliferation (КРИТИЧНО!)
**Проблема:** Много мелких файлов → хаос, устаревание, потеря информации

**Решение:** Объединять связанную информацию в ОДИН файл
- ✅ [docs/architecture.md](docs/architecture.md) = архитектура + API + chart presets + parsers guide + data flow
- ✅ [CLAUDE.md](CLAUDE.md) = все инструкции для AI в одном месте
- ✅ [docs/decisions/](docs/decisions/) = ADRs для архитектурных решений (один файл = одно решение)

**Когда создавать новый файл:**
- ✅ Отдельная техническая спецификация формата (.det, .pou) → [docs/file-formats/](docs/file-formats/)
- ✅ Архитектурное решение (ADR) → [docs/decisions/](docs/decisions/)
- ✅ Отдельная утилита/скрипт → [scripts/README.md](scripts/README.md)
- ❌ НЕ создавать: отдельные файлы для API, chart presets, parsers guide (всё в architecture.md)

### 3. README = Точка входа
- **Размер:** 50-150 строк (оптимально ~120)
- **Содержание:** Только быстрый старт
- **Детали:** Ссылки на docs/ для подробностей
- **Назначение:** Дать понять что это за проект и как быстро запустить

### 4. docs/architecture.md = Единая техническая документация
**Включает:**
- Backend архитектура (слои, модули, services, routes)
- Frontend архитектура (pages, components, hooks, state)
- API endpoints (полная документация)
- Chart presets (все 6 пресетов)
- Parsers guide (как добавлять новые форматы)
- Data flow (диаграммы взаимодействия)
- Форматы данных (TypeScript interfaces)

**НЕ создавать:** отдельные api.md, chart-presets.md, parsers-guide.md!

### 5. Не дублируй информацию!
- Если нужно повторить - сделай ссылку
- Один источник правды = легче обновлять

---

## 📁 Обязательная структура файлов (v2.0.0)

```
engine-viewer/
│
├── README.md ──────────────────────► ТОЧКА ВХОДА (50-150 строк, оптимально ~120)
│                                     • Что это (2-3 предложения)
│                                     • Быстрый старт (используя скрипты)
│                                     • Ссылки на docs/
│                                     • Технологии (React, Express, ECharts)
│
├── CLAUDE.md ──────────────────────► ИНСТРУКЦИИ ДЛЯ CLAUDE CODE
│                                     • Как работать с проектом
│                                     • Важные правила (параметры, документация)
│                                     • Структура документации
│
├── ARCHITECT-CONTEXT.md ───────────► КОНТЕКСТ ДЛЯ CLAUDE CHAT (architect mode)
│                                     • High-level архитектура
│                                     • EngMod4T Suite контекст
│                                     • Цели и ограничения проекта
│
├── CHANGELOG.md ───────────────────► ИСТОРИЯ ИЗМЕНЕНИЙ
│                                     • Semantic Versioning (v2.0.0)
│                                     • Что добавили (Added)
│                                     • Что изменили (Changed)
│                                     • Что починили (Fixed)
│                                     ⚠️ ОБНОВЛЯТЬ при significant changes!
│
├── DOCUMENTATION_GUIDE.md ─────────► ЭТОТ ФАЙЛ (правила документации!)
│                                     • Принципы SSOT + Consolidation
│                                     • Чек-листы при изменениях
│                                     • Как предотвратить хаос
│
├── AUDIT-FINDINGS.md ──────────────► РЕЗУЛЬТАТЫ АУДИТА (snapshot)
│                                     • Что реализовано
│                                     • Слабые места
│                                     • Открытые вопросы
│                                     • Технический долг
│
├── config.yaml ────────────────────► КОНФИГУРАЦИЯ ПРИЛОЖЕНИЯ
│                                     • server.port
│                                     • files.path (путь к .det файлам)
│                                     • files.extensions (поддерживаемые форматы)
│
├── .gitignore ─────────────────────► ЧТО НЕ КОММИТИТЬ
│                                     • node_modules/, dist/, logs/
│
├── package.json ───────────────────► NPM ЗАВИСИМОСТИ
│
├── scripts/ ───────────────────────► SHELL СКРИПТЫ УПРАВЛЕНИЯ
│   ├── start.sh                    • Запуск Backend + Frontend
│   ├── stop.sh                     • Остановка всех процессов
│   ├── restart.sh                  • Перезапуск проекта
│   ├── status.sh                   • Проверка статуса
│   └── README.md                   • Документация по скриптам
│
├── _personal/ ─────────────────────► ЛИЧНЫЕ ЗАМЕТКИ (игнорируется Claude)
│   └── ...                         • Твои заметки, шпаргалки, курсы
│
├── backend/ ───────────────────────► BACKEND (EXPRESS)
│   ├── package.json                • Backend dependencies
│   ├── src/
│   │   ├── server.js               • Express сервер
│   │   ├── config.js               • Загрузка config.yaml
│   │   ├── services/
│   │   │   ├── fileParser.js       • Парсер .det файлов
│   │   │   ├── fileScanner.js      • Сканирование папки
│   │   │   ├── metadataService.js  • Управление метаданными проектов
│   │   │   └── parsers/            • Парсеры для будущих форматов (опционально)
│   │   │       └── xyzParser.js    • Пример парсера нового формата
│   │   ├── routes/
│   │   │   ├── projects.js         • GET /api/projects
│   │   │   ├── data.js             • GET /api/project/:id
│   │   │   └── metadata.js         • GET/POST /api/project/:id/metadata
│   │   └── types/
│   │       └── engineData.ts       • TypeScript интерфейсы
│
├── frontend/ ──────────────────────► FRONTEND (REACT + VITE)
│   ├── package.json                • Frontend dependencies
│   ├── index.html                  • HTML разметка
│   ├── vite.config.ts              • Vite конфигурация
│   ├── tailwind.config.js          • TailwindCSS конфигурация
│   └── src/
│       ├── App.tsx                 • Main React компонент
│       ├── pages/
│       │   ├── HomePage.tsx        • Главная страница (список проектов)
│       │   └── ProjectPage.tsx     • Страница проекта (графики + таблица)
│       ├── components/
│       │   ├── ProjectList.tsx     • Список проектов
│       │   └── visualization/      • Компоненты визуализации
│       │       ├── ChartPreset1.tsx • График P-Av & Torque
│       │       ├── ChartPreset2.tsx • График PCylMax
│       │       ├── ChartPreset3.tsx • График TCylMax
│       │       ├── ChartPreset4.tsx • График Deto
│       │       ├── DataTable.tsx    • Таблица данных
│       │       └── ChartExportButtons.tsx • Кнопки экспорта
│       ├── hooks/
│       │   └── useChartExport.ts   • Hook для экспорта графиков
│       ├── api/
│       │   └── client.ts           • Axios API client
│       ├── utils/
│       │   ├── chartUtils.ts       • Утилиты для графиков
│       │   └── tableUtils.ts       • Утилиты для таблиц
│       └── types/
│           └── engineData.ts       • TypeScript интерфейсы
│
├── test-data/ ─────────────────────► ТЕСТОВЫЕ .DET ФАЙЛЫ
│   ├── Vesta 1.6 IM.det            • Тестовый файл 1
│   └── BMW M42.det                 • Тестовый файл 2
│
└── docs/ ──────────────────────────► ДЕТАЛЬНАЯ ДОКУМЕНТАЦИЯ
    │
    ├── architecture.md ────────────► ЕДИНАЯ ТЕХНИЧЕСКАЯ ДОКУМЕНТАЦИЯ (SSOT)
    │                                 ⚠️ ВКЛЮЧАЕТ ВСЁ: архитектуру, API, chart presets,
    │                                    parsers guide, data flow, паттерны
    │                                 • Backend архитектура (слои, services, routes)
    │                                 • Frontend архитектура (pages, components, hooks)
    │                                 • API endpoints (полная документация)
    │                                 • Chart presets (все 6 пресетов)
    │                                 • Parsers guide (как добавлять форматы)
    │                                 • Data flow (диаграммы)
    │                                 • Форматы данных (TypeScript interfaces)
    │                                 • Parser System (Registry Pattern)
    │                                 • Metadata System (auto/manual separation)
    │                                 • File Scanner (recursive scan)
    │                                 • Responsive Design & Accessibility
    │
    ├── engmod4t-suite/ ────────────► ДОКУМЕНТАЦИЯ ENGMOD4T SUITE
    │   └── README.md               • DAT4T, EngMod4T, Post4T (AI-friendly)
    │
    ├── setup.md ───────────────────► ДЕТАЛЬНАЯ УСТАНОВКА
    │                                 • Prerequisites (Node.js 18+)
    │                                 • Installation (npm install)
    │                                 • Running (используя scripts/)
    │                                 • Проверка работы
    │
    ├── troubleshooting.md ─────────► ПРОБЛЕМЫ И РЕШЕНИЯ
    │                                 • Частые ошибки
    │                                 • CORS issues
    │                                 • Parsing errors
    │                                 • Performance problems
    │
    ├── decisions/ ─────────────────► ADR (Architecture Decision Records)
    │   │                             ⚠️ КРИТИЧЕСКИ ВАЖНО: Документируй ПОЧЕМУ!
    │   │                             ADR помогают вспомнить причины решений
    │   ├── template.md             • Шаблон для новых ADR
    │   ├── 001-det-file-format.md  • Парсинг .det файлов
    │   ├── 002-pou-file-format.md  • Поддержка .pou + Parser Registry
    │   ├── 003-color-palette-engineering-style.md • Цветовая палитра
    │   ├── 004-engmod4t-suite-documentation.md    • AI-friendly docs
    │   └── 005-prt-parser-metadata-separation.md  • .prt парсер + auto/manual
    │
    └── file-formats/ ──────────────► ОПИСАНИЯ ФОРМАТОВ ФАЙЛОВ
        ├── README.md               • Обзор всех поддерживаемых форматов
        ├── det-format.md           • Детальное описание .det формата
        └── examples/               • Примеры файлов для каждого формата
            └── sample.det          • Образец .det файла
```

---

## 📝 Шаблоны файлов

### README.md (50-150 строк, оптимально ~120)

```markdown
# Engine Results Viewer

Веб-приложение для визуализации и анализа результатов инженерных расчётов
двигателей внутреннего сгорания (ДВС).

**Входные данные:** .det файлы с результатами расчётов
**Выходные данные:** Интерактивные графики и таблицы

## Быстрый старт

### Требования
- Node.js 18+
- npm
- macOS/Linux (Windows - с адаптацией скриптов)

### Установка

```bash
# Backend
cd backend && npm install

# Frontend
cd frontend && npm install
```

### Конфигурация

Создай `config.yaml` в корне проекта:
```yaml
server:
  port: 3000

files:
  path: ./test-data
  extensions:
    - .det
  maxSize: 10485760  # 10 MB
```

### Запуск

**Используй shell скрипты для управления проектом:**

```bash
# Запуск Backend + Frontend
./scripts/start.sh

# Проверка статуса
./scripts/status.sh

# Остановка
./scripts/stop.sh

# Перезапуск
./scripts/restart.sh
```

**Или вручную (2 терминала):**
```bash
# Терминал 1: Backend
cd backend && npm run dev

# Терминал 2: Frontend
cd frontend && npm run dev
```

**URLs:**
- Backend API: http://localhost:3000
- Frontend: http://localhost:5173

## Документация

- [Детальная установка](docs/setup.md)
- [Управление проектом (скрипты)](scripts/README.md)
- [Полная архитектура](docs/architecture.md) (включает API, chart presets, parsers guide)
- [Troubleshooting](docs/troubleshooting.md)

## Технологии

**Backend:**
- Express 4.18+
- Node.js 18+

**Frontend:**
- React 18.2+
- Vite 5.0+
- ECharts 5.5+
- TailwindCSS 3.4+
- Tanstack Table 8.11+

**Парсинг:**
- Multi-format parser system (.det, .pou, .prt)

## Changelog

См. [CHANGELOG.md](CHANGELOG.md)

## Лицензия

MIT
```

---

### Feature Roadmaps (опционально, только для крупных фич)

**Когда создавать:**
- Крупная фича с множеством задач (10+ часов работы)
- Обсуждена с Claude Chat (architect mode)
- Требует пошагового плана

**Структура:**
```markdown
# Roadmap: [Название фичи]

## Цель
[Краткое описание что делаем и зачем]

## Задачи
- [ ] Задача 1 (1-3 часа)
- [ ] Задача 2 (1-3 часа)
...

## Результат
[После завершения: что получилось, какие файлы изменены]
```

**После завершения:**
- ✅ Roadmap архивируется (можно удалить или переместить в _archive/)
- ✅ Изменения документируются в CHANGELOG.md
- ✅ НИКОГДА не храним старые roadmap в корне проекта!

---

### CHANGELOG.md

```markdown
# Changelog

Все значительные изменения проекта документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/),
версионирование следует [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Planned
- fileScanner.js для сканирования .det файлов
- API routes для проектов
- Frontend базовая структура

## [0.2.0] - 2025-10-21

### Added
- Парсер .det файлов (fileParser.js)
- TypeScript типы для данных двигателя
- Тестовые скрипты для парсера
- Поддержка произвольных названий расчетов

### Changed
- Логика парсинга маркеров $ (id vs name)

### Fixed
- Обработка названий с пробелами ("3.1 R 0.86")

## [0.1.0] - 2025-10-21

### Added
- Базовая структура проекта
- Backend структура (Express)
- Конфигурация (config.yaml)
- server.js с CORS и logging
```

---

### docs/decisions/template.md (ADR)

```markdown
# ADR [NUMBER]: [TITLE]

**Дата:** YYYY-MM-DD
**Статус:** [Предложено / Принято / Отклонено]
**Автор:** [Claude Code / User]

## Контекст

[Опиши проблему или вопрос, который требует решения]

Например:
"Нужно выбрать библиотеку для отображения графиков двигателя.
Требования: 60 FPS, множественные графики, zoom/pan функции."

## Решение

[Что решили делать]

Например:
"Использовать ECharts v5.5+ для отображения графиков."

## Причины

1. **Производительность:** ECharts оптимизирован для больших datasets
2. **Функционал:** Zoom, pan, tooltips из коробки
3. **Интеграция:** Официальный echarts-for-react wrapper
4. **Документация:** Отличная документация на русском

## Последствия

**Плюсы:**
- ✅ Высокая производительность
- ✅ Богатый функционал
- ✅ Активная поддержка

**Минусы:**
- ⚠️ Больший bundle size чем у lightweight библиотек
- ⚠️ Требует изучения API

## Альтернативы

### Chart.js
**Отклонено:** Менее производителен для больших datasets

### Plotly.js
**Отклонено:** Слишком тяжёлый bundle size

## Ссылки

- [ECharts Documentation](https://echarts.apache.org/)
- [echarts-for-react](https://github.com/hustcc/echarts-for-react)
```

---

## ✅ Чек-лист при изменениях

### Когда меняешь код:

**1. Добавил новую фичу/класс/модуль?**
- [ ] Обнови [docs/architecture.md](docs/architecture.md) (если меняет архитектуру)
- [ ] Добавь комментарии в код (JSDoc формат)
- [ ] Добавь в [CHANGELOG.md](CHANGELOG.md) (что изменилось)
- [ ] **Создай ADR** если принял важное техническое решение
  * ⚠️ ADR КРИТИЧЕСКИ ВАЖНЫ для запоминания причин решений!
  * Используй [docs/decisions/template.md](docs/decisions/template.md)
  * Документируй ПОЧЕМУ, а не ЧТО

**2. Добавил новый формат файлов (.xyz)?**
- [ ] ⚠️ **Следуй Parser System guide в [docs/architecture.md](docs/architecture.md)** - обязательно!
- [ ] Создай ADR в `docs/decisions/00X-xyz-file-format.md`
- [ ] Создай описание в `docs/file-formats/xyz-format.md`
- [ ] Обнови `docs/file-formats/README.md` (добавь строку в таблицу)
- [ ] Создай парсер в `backend/src/parsers/formats/xyzParser.js`
- [ ] Зарегистрируй в `backend/src/parsers/ParserRegistry.js`
- [ ] Обнови `config.yaml` (добавь .xyz в extensions)
- [ ] Протестируй на реальных файлах
- [ ] Проверь performance (время парсинга)

**3. Завершил крупную фичу?**
- [ ] Обнови [CHANGELOG.md](CHANGELOG.md) (major milestone, semantic versioning)
- [ ] Архивируй roadmap (если был создан для фичи)
- [ ] Проверь что [docs/architecture.md](docs/architecture.md) актуален

**4. Добавил/изменил npm зависимость?**
- [ ] Обнови package.json (автоматически через npm install)
- [ ] Упомяни в [docs/setup.md](docs/setup.md)
- [ ] Обнови README.md → Технологии (если важная зависимость)
- [ ] Добавь в [CHANGELOG.md](CHANGELOG.md) → Added/Changed

**5. Обнаружил проблему/баг?**
- [ ] Добавь в [docs/troubleshooting.md](docs/troubleshooting.md):
  * Описание проблемы
  * Как воспроизвести
  * Решение
- [ ] Если исправил - добавь в [CHANGELOG.md](CHANGELOG.md) → Fixed

**6. Изменил API endpoints?**
- [ ] Обнови [docs/architecture.md](docs/architecture.md) (секция API Routes)
- [ ] Добавь примеры запросов/ответов
- [ ] Обнови [CHANGELOG.md](CHANGELOG.md) → Changed

**7. Изменил формат данных или структуру парсера?**
- [ ] Обнови [docs/file-formats/*.md](docs/file-formats/)
- [ ] Обнови [docs/architecture.md](docs/architecture.md) (Parser System section)
- [ ] Создай ADR с объяснением изменения
- [ ] Протестируй на всех тестовых файлах

---

## ⚠️ Что НЕЛЬЗЯ делать

❌ **НЕ ПЕРЕВОДИ названия параметров из .det файла:**
- **СТРОГОЕ ПРАВИЛО:** Все названия параметров ВСЕГДА остаются в оригинале (английский)
- ❌ ЗАПРЕЩЕНО: "Мощность", "Момент", "Давление", "Температура"
- ✅ ОБЯЗАТЕЛЬНО: "P-Av", "Torque", "PCylMax", "TCylMax", "TUbMax", "PurCyl", "Deto", "Convergence", "RPM"
- **Применяется везде:** UI (графики, таблицы, легенды), TypeScript типы, компоненты
- **Исключения:** Комментарии в коде и документация на русском (README, CLAUDE.md)
- **Причина:** Точность, консистентность, профессионализм, узнаваемость
- **См. детали:** [CLAUDE.md - Правило о названиях параметров](CLAUDE.md#критически-важно-никогда-не-переводи-названия-параметров)

❌ **НЕ создавай дублирующие файлы:**
- Нет QUICKSTART.md (если есть README)
- Нет PROJECT_SUMMARY.md (если есть README + tech spec)
- Нет README в подпапках (backend/README.md, frontend/README.md)
- Нет дублирующих конфигураций
- ⚠️ Исключение: scripts/README.md - разрешён (документация скриптов)

❌ **НЕ копируй информацию:**
- Вместо копирования делай ссылку
- Один источник правды!
- Пример: формат .det файлов живет в [docs/file-formats/det-format.md](docs/file-formats/det-format.md)

❌ **НЕ делай README длинным:**
- Максимум 120 строк
- Детали → в [docs/](docs/)
- Вся техническая документация → в [docs/architecture.md](docs/architecture.md)

❌ **НЕ забывай обновлять:**
- Изменил код → обнови [docs/architecture.md](docs/architecture.md) (если меняет архитектуру)
- Завершил крупную фичу → обнови [CHANGELOG.md](CHANGELOG.md)
- Нашел баг → добавь в [docs/troubleshooting.md](docs/troubleshooting.md)
- Добавил API → обнови [docs/architecture.md](docs/architecture.md) (секция API Routes)

❌ **НЕ создавай много отдельных файлов:**
- Проблема: 30+ мелких файлов → хаос, потеря информации
- Решение: Consolidation over Proliferation (см. принцип 2)
- Всё в [docs/architecture.md](docs/architecture.md): API + chart presets + parsers guide + data flow

❌ **НЕ коммить в Git:**
- `node_modules/` (npm зависимости)
- `dist/` (compiled code)
- `logs/` (логи)
- `.env` (secrets)
- Проверь что `.gitignore` актуален

---

## 💡 Главное правило

> **Документация пишется ОДИН РАЗ правильно, обновляется при каждом изменении кода.**

### Процесс:

1. **Создаешь базовую структуру** (по шаблонам выше)
2. **При каждом изменении кода** обновляешь нужные файлы
3. **Следуешь принципу SSOT** (один источник правды)
4. **Проверяешь чек-лист** перед коммитом

### Если не знаешь куда добавить информацию:

- **Быстрый старт?** → [README.md](README.md)
- **Детальная установка?** → [docs/setup.md](docs/setup.md)
- **Как устроена система?** → [docs/architecture.md](docs/architecture.md)
- **API endpoints?** → [docs/architecture.md](docs/architecture.md) (секция API Routes)
- **Chart presets?** → [docs/architecture.md](docs/architecture.md) (секция Chart Implementation)
- **Parser System?** → [docs/architecture.md](docs/architecture.md) (секция Parser System)
- **Что-то не работает?** → [docs/troubleshooting.md](docs/troubleshooting.md)
- **Управление проектом (скрипты)?** → [scripts/README.md](scripts/README.md)
- **Добавляю новый формат файла?** → [docs/architecture.md](docs/architecture.md) (Parser System guide) + ADR
- **Описание формата файла?** → [docs/file-formats/format-name.md](docs/file-formats/)
- **Важное техническое решение?** → [docs/decisions/NNN-название.md](docs/decisions/) (ADR)
- **История изменений?** → [CHANGELOG.md](CHANGELOG.md)
- **Инструкции для Claude Code?** → [CLAUDE.md](CLAUDE.md)
- **Контекст для Claude Chat?** → [ARCHITECT-CONTEXT.md](ARCHITECT-CONTEXT.md)

---

## 🔍 Как проверить SSOT

### Команды для поиска дублирования:

```bash
# Найти все упоминания ".det файлов"
grep -r "\.det" . --exclude-dir=node_modules --exclude-dir=dist

# Найти все упоминания "ECharts"
grep -r "ECharts" . --exclude-dir=node_modules --exclude-dir=dist

# Найти все README файлы
find . -name "README.md" | grep -v node_modules

# Найти все .md файлы в корне
ls *.md

# Найти дублирующие npm скрипты
cat package.json | grep -A 10 '"scripts"'
```

### Признаки нарушения SSOT:

- ⚠️ Одна и та же информация в разных файлах
- ⚠️ Несколько README.md в подпапках
- ⚠️ Файлы типа QUICKSTART.md, GETTING_STARTED.md дублируют README
- ⚠️ Числа/метрики различаются в разных местах (roadmap vs CHANGELOG)
- ⚠️ Устаревшие чекбоксы в roadmap (задача выполнена, но не отмечена)

---

## 📚 Примеры правильной документации

### ✅ Правильно (SSOT):

**docs/file-formats/det-format.md (источник истины):**
```markdown
## Структура .det файла

- Строка 1: Метаданные (NumCylinders EngineType)
- Строка 2: Заголовки колонок (24 параметра)
- Строка 3+: Маркеры расчётов ($1, $2...) и данные
```

**README.md (краткая ссылка):**
```markdown
**Входные данные:** .det файлы с результатами расчётов

См. [формат данных](docs/file-formats/det-format.md).
```

**docs/architecture.md (как парсится):**
```markdown
## Parser System

detParser.js:
- Line 1: Extract metadata (cylinders, engineType)
- Line 2: Extract column headers (24 parameters)
- Line 3+: Extract calculations ($1, $2...) and data points
```

➡️ Информация о .det файлах в 3 местах, но контекст разный (спецификация, ссылка, реализация)

### ❌ Неправильно (дублирование):

**README.md:**
```markdown
## Установка
npm install
npm run dev
```

**QUICKSTART.md:**
```markdown
## Быстрый старт
npm install
npm run dev
```

➡️ Полное дублирование! Удали QUICKSTART.md, оставь только README.md.

---

## 🎓 Итог

**Запомни:**
1. **SSOT** - один источник правды
2. **Consolidation over Proliferation** - объединяй, не плоди файлы!
3. **README** - 80-120 строк, быстрый старт
4. **[docs/architecture.md](docs/architecture.md)** - ЕДИНАЯ техническая документация (всё в одном месте)
5. **Ссылки, не копирование**
6. **Обновляй после каждой значимой фичи** (architecture.md + CHANGELOG.md)
7. **Следуй шаблонам**
8. **Проверяй чек-лист** перед завершением задачи

**Специфика Engine Results Viewer v2.0.0:**
- **[docs/architecture.md](docs/architecture.md)** - ЕДИНЫЙ источник (архитектура + API + parsers guide + chart presets)
- **[CHANGELOG.md](CHANGELOG.md)** - обновлять после каждого significant change
- **Feature roadmaps** - создавать ТОЛЬКО для крупных фич (10+ часов), архивировать после завершения
- **[docs/decisions/](docs/decisions/)** (ADR) - КРИТИЧЕСКИ ВАЖНО документировать решения
  * ADR помогают вспомнить ПОЧЕМУ было принято решение
  * Разработчик всегда один (User + Claude Code)
  * ADR заменяют вопросы "Почему мы это сделали так?"
- **Названия параметров** - НИКОГДА не переводить на русский!
- **.metadata/** - в корне engine-viewer/, НЕ в C:/4Stroke/

**Разработчик: User + Claude Code (всегда вдвоём)**
- Нет multi-developer setup
- Нет CONTRIBUTING.md
- ADR критичны для запоминания (Claude может забыть контекст)

**Этот файл - твой гайд. Обращайся к нему при любых изменениях документации!**
