# DET File Format - Детальное описание

**Расширение:** `.det`
**Назначение:** Результаты расчётов двигателя внутреннего сгорания
**Статус:** ✅ Реализовано

---

## 📋 Структура файла

```
Строка 1: Метаданные (NumCylinders, EngineType)
Строка 2: Заголовки колонок (24 параметра)
Строка 3+: Маркеры расчётов ($1, $2...) + данные
```

---

## 📊 Пример файла

```
№  4 NATUR NumCyl
→  RPM  P-Av  Torque  PurCyl(1)  PurCyl(2)  PurCyl(3)  PurCyl(4)  ...
$1  1000  15.50  123.45  0.8500  0.8600  0.8700  0.8800  ...
$1  1500  22.30  150.20  0.8800  0.8700  0.8600  0.8500  ...
$2  1000  16.20  125.00  0.8700  0.8800  0.8900  0.8600  ...
```

---

## 🔍 Строка 1: Метаданные

**Формат:**
```
№  <NumCylinders> <EngineType> NumCyl
```

**Пример:**
```
№  4 NATUR NumCyl
```

**Параметры:**
- `№` - служебный символ (игнорируется)
- `NumCylinders` - количество цилиндров (4, 6, 8, 12)
- `EngineType` - тип двигателя (`NATUR`, `TURBO`, `SUPER`)
- `NumCyl` - служебная метка (игнорируется)

---

## 🏷️ Строка 2: Заголовки колонок

**Формат:**
```
→  RPM  P-Av  Torque  PurCyl(1)  PurCyl(2)  ...
```

**Параметры (24 колонки):**

| Параметр | Единицы | Описание |
|----------|---------|----------|
| `RPM` | об/мин | Обороты двигателя |
| `P-Av` | кВт | Средняя мощность |
| `Torque` | Н·м | Крутящий момент |
| `PurCyl(1-4)` | - | Коэффициент наполнения цилиндров |
| `TCylMax(1-4)` | °C | Макс. температура в цилиндрах |
| `PCylMax(1-4)` | бар | Макс. давление в цилиндрах |
| `TUbMax(1-4)` | °C | Макс. температура выпуска |
| `Deto(1-4)` | - | Детонация (0 = нет, >0 = есть) |
| `Convergence` | - | Сходимость расчёта |

**⚠️ Важно:** Первая колонка (→) служебная - пропускается при парсинге!

---

## 📈 Строка 3+: Данные расчётов

**Формат:**
```
<Marker>  <RPM>  <P-Av>  <Torque>  <PurCyl(1)>  ...
```

**Пример:**
```
$1  1000  15.50  123.45  0.8500  0.8600  ...
```

**Маркеры расчётов:**
- Простые: `$1`, `$2`, `$3`
- С точкой: `$3.1`, `$9.3`
- Сложные: `$3.1 R 0.86`, `$2.1 0.86 _R`

**Символ $:** Показывает начало нового расчёта.

---

## 🔄 Преобразование в JSON

**Исходный .det файл:**
```
№  4 NATUR NumCyl
→  RPM  P-Av  Torque
$1  1000  15.50  123.45
$1  1500  22.30  150.20
```

**JSON на выходе:**
```json
{
  "metadata": {
    "cylinders": 4,
    "engineType": "NATUR"
  },
  "calculations": [
    {
      "id": "$1",
      "name": "$1",
      "data": [
        { "RPM": 1000, "P-Av": 15.5, "Torque": 123.45 },
        { "RPM": 1500, "P-Av": 22.3, "Torque": 150.2 }
      ]
    }
  ]
}
```

---

## 🎯 Особенности формата

### 1. Служебная первая колонка

**Проблема:** Первая колонка содержит символы `№`, `→` и номера строк.

**Решение:** Парсер пропускает первую колонку при обработке всех строк.

```javascript
const values = line.split(/\t+/).slice(1);  // ← slice(1)!
```

### 2. Сложные маркеры

**Проблема:** Маркеры могут содержать пробелы и спецсимволы.

**Примеры:**
- `$3.1 R 0.86` → id = "3.1 R 0.86"
- `$2.1 0.86 _R` → id = "2.1 0.86 _R"

**Решение:** Сохраняем весь текст после символа `$` как id расчёта.

### 3. Массивы параметров

**Проблема:** Параметры `PurCyl(1-4)` разворачиваются в 4 колонки.

**Решение:** Группируем по имени параметра в массив.

```javascript
// В файле:
PurCyl(1)  PurCyl(2)  PurCyl(3)  PurCyl(4)
0.85       0.86       0.87       0.88

// В JSON:
"PurCyl": [0.85, 0.86, 0.87, 0.88]
```

### 4. Вариативное количество точек

**Проблема:** Разные расчёты могут иметь разное количество строк (25-30).

**Решение:** Читаем до конца файла или до следующего маркера `$`.

---

## 📁 Примеры файлов

### Vesta 1.6 IM.det

- **Расчётов:** 17
- **Строк:** 462
- **Маркеры:** $1, $2, $3, $3.1, $3.1 R 0.86, и др.

### BMW M42.det

- **Расчётов:** 30
- **Строк:** 804
- **Маркеры:** $1-$9, $9.1-$9.3

---

## 🔗 Ссылки

- [ADR 001: Парсинг .det файлов](../decisions/001-det-file-format.md)
- [Парсер: backend/src/services/fileParser.js](../../backend/src/services/fileParser.js)
- [Типы: shared-types.ts](../../shared-types.ts)
- [API: GET /api/project/:id](../api.md)
