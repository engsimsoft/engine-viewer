{
  "name": "engine-viewer",
  "version": "3.4.0",
  "description": "Engine simulation results visualization tool",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "dependencies": {
    "html-to-image": "^1.11.13"
  }
}
