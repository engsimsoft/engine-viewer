# 📋 Техническое Задание: Engine Results Viewer

**Версия:** 2.1 Final  
**Дата:** 21 октября 2025  
**Для:** Claude Code (VS Code)

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО ПЕРЕД НАЧАЛОМ

### 🔍 Обязательное изучение документации и best practices

**Перед написанием любого кода:**

1. **Изучи официальную документацию:**
   - React 18: https://react.dev/
   - ECharts: https://echarts.apache.org/en/index.html
   - echarts-for-react: https://github.com/hustcc/echarts-for-react
   - TypeScript: https://www.typescriptlang.org/docs/
   - Vite: https://vitejs.dev/guide/
   - TailwindCSS: https://tailwindcss.com/docs
   - Express.js: https://expressjs.com/

2. **Изучи best practices:**
   - React best practices (2025)
   - TypeScript best practices
   - ECharts performance optimization
   - Modern React patterns (hooks, composition)
   - Project structure for React + TypeScript

3. **Особое внимание:**
   - ECharts configuration best practices
   - React performance optimization
   - TypeScript strict mode
   - Vite configuration для production

**Цель:** Написать код на уровне senior developer, используя современные подходы и паттерны 2025 года.

**НЕ начинай писать код, пока не изучишь документацию!**

---

## 🔍 ПЕРВЫЙ ШАГ: ИЗУЧЕНИЕ ТЕСТОВОГО ФАЙЛА

### ⚠️ ОБЯЗАТЕЛЬНО перед написанием кода:

**1. Открой файл:**
```bash
test-data/Vesta 1.6 IM.det
```

**2. Изучи структуру построчно:**
- **Строка 1:** метаданные (тип двигателя, цилиндры)
- **Строка 2:** названия колонок
- **Строка 3+:** маркеры расчётов ($1, $2...) и данные

**3. Выведи в консоль:**
```javascript
// Анализ файла
console.log('=== Анализ файла Vesta 1.6 IM.det ===');
console.log('Все уникальные маркеры расчётов:', markers);
console.log('Полный список колонок:', columns);
console.log('Количество точек данных на расчёт:', dataPoints);
console.log('Пример первой строки данных:', firstDataRow);
```

**4. Создай типы TypeScript на основе реальных данных:**
```typescript
// Типы ДОЛЖНЫ соответствовать реальной структуре файла!
interface EngineData {
  // Определи на основе реальных колонок
}
```

**5. Напиши парсер с учётом реальной структуры**

### 🚨 КРИТИЧЕСКИ ВАЖНО:

**Первая колонка - это номер строки (служебная).**  
**Данные начинаются со ВТОРОЙ колонки!**

**Не делай предположений о структуре - изучи файл!**

---

## 🎯 1. ЦЕЛЬ ПРОЕКТА

### Основная цель
Веб-приложение для визуализации и анализа результатов инженерных расчётов двигателей внутреннего сгорания.

### Роль проекта
**Тренажёр для освоения современных UI технологий**, которые будут использованы в будущем проекте ECU Software (управление электронными блоками двигателя).

### Ключевые принципы
- Современный код (React 18, TypeScript, ECharts)
- Best practices везде
- Чистая архитектура
- Готовность к масштабированию

---

## 🖥️ 2. СРЕДА РАЗРАБОТКИ

### Платформа
```
ОС: macOS
IDE: VS Code с Claude Code
Тестирование: Chrome/Safari (локально)
```

### Этапность
**Этап 1 (сейчас):** Разработка и тестирование на macOS  
**Этап 2 (потом):** Упаковка в EXE и тест на Windows 10/11  
**Этап 3 (в конце):** Поддержка старых систем (Node.js 16)

**Фокус:** Работающее веб-приложение на macOS. НЕ думать об EXE!

---

## 🏗️ 3. ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Backend
```json
{
  "node": "18.x",
  "express": "^4.18.0",
  "cors": "^2.8.5",
  "js-yaml": "^4.1.0",
  "chokidar": "^3.5.3"
}
```

### Frontend
```json
{
  "core": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "charts": {
    "echarts": "^5.5.0",
    "echarts-for-react": "^3.0.2"
  },
  "ui": {
    "tailwindcss": "^3.4.0",
    "lucide-react": "^0.400.0",
    "@tanstack/react-table": "^8.11.0"
  },
  "utils": {
    "axios": "^1.6.0",
    "date-fns": "^3.0.0",
    "clsx": "^2.1.0"
  }
}
```

**Требования:**
- TypeScript в strict mode
- Современные React patterns (только функциональные компоненты)
- ECharts настроенный по best practices
- Tailwind utility-first подход

---

## 📁 4. СТРУКТУРА ПРОЕКТА

```
engine-results-viewer/
│
├── backend/
│   ├── src/
│   │   ├── server.js           # Express сервер
│   │   ├── config.js           # Загрузка config.yaml
│   │   ├── fileScanner.js      # Сканирование папки
│   │   ├── fileParser.js       # Парсинг файлов
│   │   └── routes/
│   │       ├── projects.js
│   │       └── data.js
│   ├── package.json
│   └── .gitignore
│
├── frontend/
│   ├── src/
│   │   ├── components/         # React компоненты
│   │   ├── pages/              # Страницы
│   │   ├── hooks/              # Custom hooks
│   │   ├── types/              # TypeScript типы
│   │   ├── utils/              # Утилиты
│   │   ├── api/                # API клиент
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.css
│   ├── public/
│   ├── index.html
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   └── .gitignore
│
├── test-data/
│   └── Vesta 1.6 IM.det        # Тестовый файл
│
├── config.yaml
├── TECH_SPEC.md                # Этот файл
└── README.md
```

**Примечание:** Структуру компонентов определи сам, используя best practices для React + TypeScript проектов.

---

## 📊 5. ВХОДНЫЕ ДАННЫЕ

### Тестовый файл

**Расположение:** `test-data/Vesta 1.6 IM.det`

**ВАЖНО:**
- Файл находится в корне проекта в папке `test-data/`
- Расширение файла: `.det` (всегда будет именно такое расширение)
- Файл можно открыть любым текстовым редактором
- Это обычный текстовый файл с данными

**КРИТИЧНО:**
- **Первая колонка - служебная (номер строки)**
- **Данные начинаются со ВТОРОЙ колонки!**
- Не делай предположений - изучи файл перед парсингом!

**Задачи:**
1. ✅ Открой файл `test-data/Vesta 1.6 IM.det`
2. ✅ Изучи структуру построчно
3. ✅ Выведи в консоль анализ файла
4. ✅ Создай типы TypeScript на основе реальных данных
5. ✅ Напиши парсер с учётом реальной структуры

### Формат файла

**Общая структура (изучи сам файл!):**
```
Строка 1: Заголовок (количество цилиндров, тип двигателя)
Строка 2: Названия колонок (RPM, P-Av, Torque, ...)
Строка 3+: Маркер расчёта ($1, $2, $3...) и данные
```

**Особенности:**
- Первая колонка - служебная
- Разделители: пробелы/табуляция
- Маркеры расчётов: `$1`, `$2`, `$3`, `$3.1`, `$3.1 R 0.86` и т.д.
- Числа: целые и с плавающей точкой
- Могут быть пустые строки (игнорировать)

### Параметры в файле

**ИЗУЧИ файл для полного списка параметров!**

Основные параметры (примеры):
- RPM - обороты двигателя
- P-Av - средняя мощность
- Torque - крутящий момент
- PurCyl(1-4) - коэффициент наполнения цилиндров
- TUbMax(1-4) - максимальная температура выпуска
- TCylMax(1-4) - максимальная температура цилиндров
- PCylMax(1-4) - максимальное давление
- Deto(1-4) - детонация
- Convergence - сходимость расчёта

### Формат вывода парсера (JSON)

```json
{
  "project_name": "Vesta 1.6 IM",
  "file_path": "test-data/Vesta 1.6 IM.det",
  "modified_date": "2025-10-21T10:30:00Z",
  "engine": {
    "type": "NATUR",
    "cylinders": 4
  },
  "parameters": ["RPM", "P-Av", "Torque", ...],
  "calculations": [
    {
      "id": "$1",
      "name": "$1",
      "data": [
        { "RPM": 2600, "P-Av": 33.69, "Torque": 123.73, ... },
        { "RPM": 2800, "P-Av": 41.92, "Torque": 142.97, ... }
      ]
    }
  ]
}
```

---

## ⚙️ 6. КОНФИГУРАЦИЯ

### config.yaml

```yaml
# Путь к папке с файлами
files:
  # Для тестирования используй: "./test-data"
  path: "./test-data"
  
  # Расширение файлов: .det
  extensions: [".det"]
  
  scan_on_startup: true
  watch_interval: 5

# Сервер
server:
  host: "localhost"
  port: 3000
  auto_open_browser: false  # Пока false

# UI
ui:
  max_calculations_compare: 5
  default_preset: "power_torque"
  language: "ru"

# Цвета расчётов
colors:
  calculation_1: "#ff6b6b"
  calculation_2: "#4ecdc4"
  calculation_3: "#45b7d1"
  calculation_4: "#f9ca24"
  calculation_5: "#a29bfe"

# Графики
charts:
  theme: "light"
  animation: true
  show_grid: true
  export_format: "png"
```

---

## 🎨 7. ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

### 7.1. Главная страница

**Что показывать:**
- Список всех проектов (файлов `.det`) из папки `test-data/`
- Для каждого: название, количество расчётов, дата изменения
- Кнопка "Открыть" → переход к визуализации
- Если файлов нет → показать пустое состояние

**Сортировка:** По дате (новые сверху)

---

### 7.2. Страница визуализации

**Элементы интерфейса:**

1. **Селектор расчётов**
   - Чекбоксы для выбора расчётов ($1, $2, $3...)
   - Максимум 5 одновременно
   - Цветная метка рядом с каждым (из config.yaml)

2. **Пресеты графиков** (4 кнопки)
   - Пресет 1: "Мощность и момент" (P-Av + Torque vs RPM)
   - Пресет 2: "Давление в цилиндрах" (PCylMax(1-4) vs RPM)
   - Пресет 3: "Температурный режим" (TCylMax + TUbMax vs RPM)
   - Пресет 4: "Кастом" (пользователь выбирает параметры)

3. **Графики (ECharts)**
   - Интерактивные: zoom (колёсико), pan (перетаскивание)
   - Tooltip при наведении
   - Slider внизу для выбора диапазона
   - Легенда (клик = скрыть/показать линию)
   - Кнопки экспорта (PNG, SVG)

4. **Таблица данных**
   - Под графиками
   - Сортировка по колонкам
   - Фильтр по расчёту
   - Экспорт в CSV/Excel

---

## 🌐 8. API (Backend)

### Endpoints

**GET /api/projects**
- Список всех проектов (файлов `.det`) в папке `test-data/`

**GET /api/project/:id**
- Данные конкретного проекта (результат парсинга файла `.det`)

**GET /api/config**
- Текущая конфигурация

**POST /api/config**
- Обновление конфигурации

**Примечание:** Структуру ответов определи сам, используя best practices для REST API.

---

## 🚀 9. РАЗРАБОТКА

### Начало работы

**1. Изучи тестовый файл:**
```bash
# Открой файл в VS Code
code test-data/Vesta\ 1.6\ IM.det

# Или в терминале
cat test-data/Vesta\ 1.6\ IM.det
```

**2. Пойми структуру данных:**
- Где метаданные?
- Какие колонки?
- Какие маркеры расчётов?
- Сколько точек данных?
- **Какая колонка служебная?** (первая!)

**3. Создай парсер:**
- Учти служебную колонку
- Обработай все маркеры
- Валидация данных

**4. Тестируй на файле**

### Запуск (два терминала)

**Терминал 1 - Backend:**
```bash
cd backend
npm install
npm run dev
# http://localhost:3000
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm install
npm run dev
# http://localhost:5173
# Proxy /api/* → localhost:3000
```

### Vite proxy

Настрой в `vite.config.ts`:
```typescript
server: {
  proxy: {
    '/api': 'http://localhost:3000'
  }
}
```

---

## 📝 10. ТРЕБОВАНИЯ К КАЧЕСТВУ

### Код
- ✅ TypeScript strict mode
- ✅ ESLint + Prettier
- ✅ Никаких `any` типов
- ✅ Комментарии для сложной логики
- ✅ Понятные имена переменных/функций

### Производительность
- ✅ Загрузка файла: < 2 сек
- ✅ Парсинг: < 1 сек
- ✅ Отрисовка графика: < 1 сек
- ✅ Переключение пресета: < 100 мс
- ✅ Плавность графиков: 60 FPS

### UX
- ✅ Интуитивный интерфейс
- ✅ Обработка ошибок (показывать понятные сообщения)
- ✅ Загрузочные состояния (spinners)
- ✅ Пустые состояния (нет данных)
- ✅ Адаптивный дизайн

### Архитектура
- ✅ Чистая структура компонентов
- ✅ Переиспользуемые компоненты
- ✅ Разделение логики и UI
- ✅ Custom hooks для бизнес-логики

---

## ✅ 11. ЧЕКЛИСТ

### Подготовка
- [ ] Изучить документацию всех технологий
- [ ] Изучить best practices (React, TypeScript, ECharts)
- [ ] **ИЗУЧИТЬ файл `test-data/Vesta 1.6 IM.det` построчно**
- [ ] **Вывести в консоль анализ файла**
- [ ] **Создать типы TypeScript на основе реальных данных**
- [ ] Составить план архитектуры

### Минимальная версия
- [ ] Backend: Express сервер + парсер `.det` файлов + API
- [ ] Парсер учитывает служебную колонку
- [ ] Frontend: Routing + HomePage + страница визуализации
- [ ] Один пресет графиков работает
- [ ] **Тестирование на файле `Vesta 1.6 IM.det`**

### Полная версия
- [ ] Выбор расчётов (макс. 5)
- [ ] Все 4 пресета
- [ ] Таблица данных
- [ ] Экспорт (графики, данные)
- [ ] Обработка ошибок
- [ ] UI/UX полировка

### Документация
- [ ] README.md с инструкциями по запуску
- [ ] Комментарии в коде

---

## 🎓 12. РЕКОМЕНДАЦИИ

### Подход к разработке

**1. Начни с изучения:**
- Прочитай всю документацию
- Изучи best practices
- **ИЗУЧИ файл `test-data/Vesta 1.6 IM.det` построчно!**
- **Выведи анализ в консоль перед парсингом!**

**2. Спланируй архитектуру:**
- Структура компонентов
- Типы TypeScript (на основе РЕАЛЬНЫХ данных!)
- API контракты

**3. Пиши код постепенно:**
- Сначала минимальная версия
- Потом добавляй функциональность
- **Тестируй на файле `Vesta 1.6 IM.det`**

**4. Следуй best practices:**
- Современные React patterns
- TypeScript strict mode
- Чистая архитектура
- Переиспользуемые компоненты

### Что НЕ делать

❌ Начинать писать код без изучения документации  
❌ **Делать предположения о структуре файла**  
❌ **Игнорировать служебную колонку**  
❌ Использовать устаревшие подходы (class components, jQuery)  
❌ Игнорировать TypeScript типы (`any` повсюду)  
❌ Думать об упаковке в EXE (это потом!)  
❌ Копировать код из интернета без понимания  

### Что делать

✅ Изучить официальную документацию  
✅ **ИЗУЧИТЬ файл `test-data/Vesta 1.6 IM.det` построчно**  
✅ **Вывести анализ файла в консоль**  
✅ **Учесть служебную первую колонку**  
✅ Использовать современные подходы (2025)  
✅ Писать чистый, понятный код  
✅ Следовать best practices  
✅ Тестировать на каждом этапе  

---

## 🏁 13. ИТОГО

### Цель
Работающее веб-приложение для визуализации результатов расчётов двигателей на macOS.

### Принципы
- Современный код (React 18, TypeScript, ECharts)
- Best practices везде
- Чистая архитектура
- Готовность к масштабированию

### Первый шаг
**ИЗУЧИ ДОКУМЕНТАЦИЮ, BEST PRACTICES И ТЕСТОВЫЙ ФАЙЛ!**

Не начинай писать код, пока не изучишь:
1. React 18 docs
2. ECharts docs + echarts-for-react
3. TypeScript best practices
4. Modern React patterns
5. **Файл `test-data/Vesta 1.6 IM.det` построчно!**

### КРИТИЧНО:
```
ПЕРВАЯ КОЛОНКА - СЛУЖЕБНАЯ (номер строки)
ДАННЫЕ НАЧИНАЮТСЯ СО ВТОРОЙ КОЛОНКИ!
НЕ ДЕЛАЙ ПРЕДПОЛОЖЕНИЙ - ИЗУЧИ ФАЙЛ!
```

### Файлы для начала
1. Этот файл (`TECH_SPEC.md`)
2. **Тестовый файл: `test-data/Vesta 1.6 IM.det`** ⭐
3. Официальная документация (ссылки в начале)

### Тестирование
**Всё тестирование и отладка на файле:**
```
test-data/Vesta 1.6 IM.det
```

**Расширение файлов:** `.det` (всегда)

---

**Удачи в разработке! Пиши чистый, современный код! 🚀**