# 🔧 Будущие оптимизации - Подробное объяснение

**Дата создания:** 9 ноября 2025
**Версия:** 1.0
**Для:** Инженера (не программиста)
**Связано с:** ADR-009 (Lazy .prt parsing)

---

## 1️⃣ IndexedDB/SQLite вместо JSON файлов

### Текущая ситуация

- Сейчас метаданные хранятся как **отдельные JSON файлы** (один файл = один проект)
- Каждый раз нужно **открыть файл, прочитать весь текст, распарсить JSON**
- Это как хранить данные двигателя в **35 отдельных бумажных журналах** - чтобы найти что-то, нужно взять нужный журнал, открыть, прочитать

### Что такое IndexedDB/SQLite

- Это **база данных** - как единая электронная таблица Excel
- Все проекты в **одном месте**, можно быстро искать/фильтровать
- **SQLite** = файловая база данных (один файл `.db`, внутри все проекты)
- **IndexedDB** = встроенная база браузера (работает в Firefox/Chrome)

### Аналогия

```
JSON файлы = 35 бумажных журналов на полке
SQLite     = Единая база данных в Excel с быстрым поиском
```

### Зачем это нужно

- ⚡ **Скорость**: Поиск по 500 проектам - мгновенный (вместо открытия 500 файлов)
- 🔍 **Фильтрация**: "Найти все турбо двигатели клиента Петрова" - одна команда
- 📊 **Сортировка**: "Показать 10 самых мощных двигателей" - мгновенно
- 💾 **Меньше операций**: 1 файл открыть вместо 500

### Когда внедрять

- Когда проектов **> 100-200** (сейчас 35 - JSON файлы отлично работают)
- Когда нужна **сложная фильтрация** (по мощности, клиенту, дате)
- Когда нужна **статистика** (средняя мощность всех двигателей, количество турбо проектов)

---

## 2️⃣ Background metadata refresh (Автообновление кэша)

### Текущая ситуация

- Кэш проверяется **только при старте** сервера
- Если `.prt` файл изменился **пока сервер работает** - File Watcher обнаружит
- Но если сервер **не перезапускался несколько дней** - кэш может устареть

### Что такое Background refresh

- Каждые **N часов** (например, каждые 6 часов) автоматически проверять:
  - Изменились ли `.prt` файлы?
  - Нужно ли обновить метаданные?
- Работает **в фоне**, не мешает основной работе

### Аналогия

```
Текущая система    = Проверяем документацию только при включении компьютера
Background refresh = Проверяем документацию каждые 6 часов автоматически
```

### Зачем это нужно

- 🔄 **Актуальность**: Метаданные всегда свежие
- ⏰ **Автоматизация**: Не нужно вручную перезапускать сервер
- 🛡️ **Защита от расхождений**: Если файл изменён вне системы (вручную в Windows)

### Когда внедрять

- Когда сервер работает **24/7 без перезапусков**
- Когда файлы `.prt` могут меняться **вне системы** (редактируются вручную)
- Когда важна **100% актуальность данных**

### Пример реализации

```javascript
// Каждые 6 часов проверяем все файлы
setInterval(async () => {
  console.log('[Auto-refresh] Checking for stale cache...');

  for (const file of allPrtFiles) {
    const needsUpdate = await shouldParsePrt(file.path, file.id);
    if (needsUpdate) {
      console.log(`[Auto-refresh] ${file.name} changed → re-parsing`);
      await queue.add(file);
    }
  }
}, 6 * 60 * 60 * 1000); // 6 часов в миллисекундах
```

---

## 3️⃣ Queue prioritization based on user navigation

### Текущая ситуация

- Очередь обрабатывает файлы **в порядке поступления**
- Приоритеты: File Watcher (высокий) > Startup (низкий)
- Все файлы **равнозначны** при старте

### Что такое User navigation priority

- **Умная очередь**: парсить **сначала то, что пользователь смотрит**
- Пример: пользователь открыл проект "BMW M42" → его метаданные парсятся **первыми**
- Остальные проекты - **по мере необходимости**

### Аналогия

```
Текущая система       = Загружаем все 500 чертежей подряд
Smart prioritization  = Загружаем сначала тот чертёж, который инженер открыл
```

### Зачем это нужно

- ⚡ **Instant UX**: Проект, который открыл пользователь, загружается **мгновенно**
- 🎯 **Экономия ресурсов**: Не парсим 500 файлов, если пользователь смотрит только 3
- 📱 **Лучше для медленных машин**: Сначала важное, потом остальное

### Пример работы

```
Пользователь открыл список проектов:
  → Видит 10 проектов на экране
  → Система парсит сначала эти 10 (приоритет: ВЫСОКИЙ)
  → Остальные 490 файлов парсятся в фоне (приоритет: НИЗКИЙ)

Пользователь кликнул на "BMW M42":
  → Система СРАЗУ парсит BMW M42 (приоритет: КРИТИЧЕСКИЙ)
  → Переключает очередь: BMW M42 → Проекты на экране → Остальные
```

### Когда внедрять

- Когда проектов **> 100**
- Когда важна **скорость отклика** для пользователя
- Когда пользователь работает с **несколькими проектами**, а не со всеми сразу

---

## 4️⃣ WebWorkers для тяжёлых операций

### Текущая ситуация

- Парсинг `.prt` файлов происходит на **backend** (Node.js сервер)
- Frontend **только показывает** результаты
- Всё работает быстро (278-306ms startup)

### Что такое WebWorkers

- **Параллельные процессы** в браузере
- Можно делать **тяжёлые вычисления** не блокируя интерфейс
- Как **дополнительные ядра процессора** - работают отдельно

### Аналогия

```
Обычный JavaScript = Один механик собирает двигатель
WebWorker          = 4 механика работают параллельно (каждый свою часть)
```

### Зачем это может понадобиться

- 📊 **Тяжёлые графики**: Построение графика для 10,000 точек данных
- 🔢 **Сложные расчёты**: Анализ данных прямо в браузере
- 📈 **Real-time обработка**: Фильтрация/сортировка больших массивов

### Когда внедрять

- Когда нужна **обработка данных на frontend** (не на сервере)
- Когда интерфейс **тормозит** от вычислений
- В **Phase 3+**, когда появятся сложные аналитические функции

### Пример использования

```javascript
// Вместо блокировки интерфейса:
const data = heavyCalculation(10000points); // ❌ Браузер зависнет на 2 секунды

// WebWorker работает в фоне:
const worker = new Worker('calculator.js');
worker.postMessage(10000points); // ✅ Интерфейс не зависает
worker.onmessage = (result) => {
  console.log('Расчёт готов:', result);
};
```

---

## 📊 Сводная таблица: Когда внедрять

| Оптимизация | Текущая потребность | Когда станет актуально |
|-------------|---------------------|------------------------|
| **SQLite вместо JSON** | ❌ Не нужно (35 файлов) | ✅ При > 100-200 проектах |
| **Auto-refresh** | ❌ Не нужно (File Watcher работает) | ✅ Если сервер 24/7 без рестартов |
| **Smart queue** | ❌ Не нужно (быстро и так) | ✅ При > 100 проектах + UI лагает |
| **WebWorkers** | ❌ Не нужно (парсинг на backend) | ✅ Phase 3: тяжёлая аналитика на frontend |

---

## ✅ Вывод для инженера

### Сейчас (35 проектов)

- ✅ JSON файлы - **отлично работают**
- ✅ File Watcher - **мгновенно обнаруживает изменения**
- ✅ Очередь с приоритетами - **достаточно для текущих задач**
- ✅ Backend парсинг - **быстрый и надёжный**

### Будущее (500+ проектов)

- Возможно понадобится **SQLite** для быстрого поиска/фильтрации
- Возможно понадобится **Smart queue** для мгновенного отклика UI
- **Auto-refresh** и **WebWorkers** - только если появятся специфические требования

### Рекомендация

- 🎯 **Сейчас**: Ничего не менять - всё работает отлично
- 📈 **При росте до 100+ проектов**: Рассмотреть SQLite
- 🚀 **При росте до 500+**: Рассмотреть Smart queue

---

## 🏎️ Аналогия с двигателем

Это как с двигателем: **не нужно ставить турбину на 1.6L**, если он отлично работает в NA конфигурации.

Эти оптимизации - **на случай масштабирования**, но сейчас они избыточны.

**Когда понадобится турбина?**
- Когда NA конфигурация **не справляется** с нагрузкой (> 100 проектов)
- Когда нужна **большая мощность** (сложная фильтрация, статистика)
- Когда появятся **новые требования** (24/7 работа, тяжёлая аналитика)

**А пока:**
- NA двигатель (текущая система) работает **идеально**
- Быстрый запуск (278-306ms)
- Стабильная работа (0 ошибок)
- Простота обслуживания (JSON файлы понятны)

---

## 📚 Дополнительные материалы

**Ссылки на документацию:**
- ADR-009: `docs/decisions/009-lazy-prt-parsing.md` - полное техническое описание
- CHANGELOG.md - раздел "Performance" - результаты тестирования
- ROADMAP_PERFORMANCE_FIX.md - этапы оптимизации (все 8 стадий)

**Текущие метрики (после оптимизации):**
- Startup: **278-306ms** (цель < 2 сек) ✅
- Race conditions: **0 ошибок** ✅
- Concurrency: **Max 3 файла** ✅
- Memory: **Стабильная** ✅

---

**Последнее обновление:** 9 ноября 2025
**Статус:** Все оптимизации описаны, ничего не требуется внедрять сейчас
